version: '3.8'

services:
  backend:
    build: ./backend
    container_name: lifolio-backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: lifolio-frontend
    ports:
      - "80:80"
    depends_on:
      - backend